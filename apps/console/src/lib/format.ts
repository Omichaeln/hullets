export const fmtDate = (iso?: string | null) => iso ? new Date(iso).toLocaleString("en-GB", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit" }) : "–";
export const fmtDay = (iso?: string | null) => iso ? new Date(iso).toLocaleDateString("en-GB", { day: "2-digit", month: "long", year: "numeric" }) : "–";
export const ago = (iso?: string | null) => { if (!iso) return "–"; const s = Math.max(0, (Date.now() - Date.parse(iso)) / 1000); if (s < 60) return `${Math.round(s)}s ago`; if (s < 3600) return `${Math.round(s / 60)}m ago`; if (s < 86400) return `${Math.round(s / 3600)}h ago`; return `${Math.round(s / 86400)}d ago`; };
export const money = (minor?: number | null, ccy = "USD") => minor == null ? "–" : `${ccy} ${(minor / 100).toFixed(2)}`;
export const titleCase = (s?: string | null) => (s ?? "").replace(/_/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
export const STATUS_TONE: Record<string, "success" | "warning" | "danger" | "info" | "primary" | undefined> = { qualified: "success", not_qualified: "danger", duplicate: "warning", review: "info", reupload: "warning", delayed: "warning", received: undefined, processing: "info", active: "success", excluded: "danger", paused: "warning", closed: undefined, draft: undefined, archived: undefined, frozen: "info", executing: "info", executed: "warning", approved: "success", published: "primary", voided: "danger", selected: "info", notified: "info", verified: "success", accepted: "success", collected: "primary", replaced: "danger", expired: "danger", declined: "danger", ineligible: "danger", unreachable: "warning", disputed: "warning", sent: "success", delivered: "success", read: "success", pending: undefined, retryable_failure: "warning", permanent_failure: "danger", unknown_outcome: "danger", dead: "danger", failed: "warning", processed: "success", open: "warning", assigned: "info", escalated: "danger", decided: "success", reconciled: "success", critical: "danger", warning: "warning", info: "info", ok: "success", pass: "success", fail: "danger", blocking: "danger" };
export const isoDayStart = (d: Date) => new Date(Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate())).toISOString();
